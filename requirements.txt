# Direct runtime dependencies, exactly pinned and tested on Python 3.12.
# Transitive dependencies are still selected by pip's resolver; this is not a
# hash-locked, fully transitive lock file.
discord.py==2.7.1
python-dotenv==1.2.2
